version: 2
updates:
  # Python deps are managed by the pip-tools lock job (requirements-lock.yml):
  # dependabot pip PRs cannot regenerate requirements.lock and would fail CI.
  - package-ecosystem: docker
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
