name: PR52 baseline staging paused

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Baseline staging is paused while verified patch chunks are uploaded."
